#include <stdio.h>

int main() {
    char name1[100];
    printf("enter the word :");
    scanf("%s",name1);
    char name2[100];

    int i;
    for(i=0; name1[i]!='\0'; i++){
        name2[i]=name1[i];
    }
    name2[i]='\0';
    printf("copied word is %s", name2);
    return 0;
}